import {
  colors,
  fontSizes,
  fontWeights,
  fonts,
} from "@controle-devs-ui/tokens"

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    extend: {},
    fontSize: fontSizes,
    fontWeight: fontWeights,
    fontFamily: fonts,
    colors: colors,
  },
  plugins: [],
}
