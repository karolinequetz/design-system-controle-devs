import { cva } from 'class-variance-authority';

export const select = cva(' text-gray-700 dark:text-gray-500 font-medium');
export const label = cva(' bg-purple-100 ');
export const close = cva(' bg-purple-100 text-primary');
export const option = cva(' hover:bg-primary hover:text-white ');
